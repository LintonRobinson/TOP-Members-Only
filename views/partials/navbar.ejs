<nav>
    <span>Members Only</span>
    <div>
        <% if(locals.user) {%>
            <a href="/log-out">Log Out</a>
            <%} else {%>
                <a href="/log-in">Log In</a>
                <%}%>

    </div>
</nav>